# Production Environment Variables Template
# Copy this file to .env.production and replace placeholders with actual values

# Backend API Gateway URL (from CDK deployment output)
# Get this value by running: cd infrastructure && cdk deploy RepresentApp-dev --outputs-file outputs.json
# Then extract the ApiUrl value from outputs.json
VITE_API_BASE_URL=https://YOUR_API_GATEWAY_ID.execute-api.us-east-1.amazonaws.com
